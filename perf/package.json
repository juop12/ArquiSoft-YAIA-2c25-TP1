{
  "name": "perf",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "artillery": "artillery",
    "api": "artillery run rates.yaml -e api",
    "scenario": "./run-scenario.sh",
    "baseline": "artillery run baseline.yaml -e api",
    "stress": "artillery run stress.yaml -e api",
    "exchange": "artillery run exchange.yaml -e api",
    "burst": "artillery run burst.yaml -e api"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "artillery": "2.0.22",
    "artillery-plugin-statsd": "^2.2.1"
  }
}
